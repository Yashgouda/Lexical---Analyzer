# Lexical---Analyzer
A lexical analyzer (lexer) is the first phase of a compiler that scans the source program and converts it into a sequence of tokens. It removes whitespace/comments and identifies keywords, identifiers, constants, operators, and symbols for the parser.
